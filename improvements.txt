# Redball Remaining Improvements for 10/10 Score

## Core Functionality Enhancements

1. **[x] Windows Toast Notifications** (Modern Win10/11) - IMPLEMENTED
   - Send-RedballToast() function added with WinRT fallback
   - Shows native Win10/11 notifications with fallback

2. **[x] PowerShell 7 / Core Compatibility** - IMPLEMENTED
   - $script:IsPS7 detection and WinRT support check added

3. **[x] High DPI Awareness** - IMPLEMENTED
    - Detects per-monitor DPI settings
    - Uses proper DPI awareness APIs correctly on 4K displays
   - Requires: Application manifest, DPI changed event handler

## User Experience

5. **[x] Settings GUI Dialog** - IMPLEMENTED
   - Show-RedballSettingsDialog() function with tabbed interface
   - General, Smart Features, and Logging tabs
   - No need to manually edit JSON files

6. **[x] Dark Mode Support** - IMPLEMENTED
    - Detects Windows 10/11 dark theme
    - Logs dark mode statuswitching

7. **[x] Startup with Windows** - IMPLEMENTED
   - Install-RedballStartup() / Uninstall-RedballStartup() functions
   - Tray menu toggle with 'S' shortcut

8. **[x] Session Restore** - IMPLEMENTED
   - Save-RedballState() / Restore-RedballState() functions
   - Saves all settings and restores on startup

9. **[x] Minimize on Start** - IMPLEMENTED
   - -Minimized CLI parameter supported
   - Starts without showing icon initially

10. **[x] Command-Line Interface** - IMPLEMENTED
    - -Install / -Uninstall: Manage startup
    - -Duration N: Run for N minutes
    - -ExitOnComplete: Exit after duration
    - -Status: Return JSON status
    - -Minimized: Start minimized

## Smart Features

11. **[x] Battery-Aware Mode** - IMPLEMENTED
    - Get-BatteryStatus() / Update-BatteryAwareState() functions
    - Auto-pause when battery < threshold%, auto-resume on charge
    - Tray menu toggle with 'B' shortcut

12. **[x] Network-Aware Mode** - IMPLEMENTED
    - Get-NetworkStatus() / Update-NetworkAwareState() functions
    - Auto-pause when network disconnects, auto-resume on reconnect
    - Tray menu toggle with 'N' shortcut

13. **[x] Idle Detection Enhancement** - IMPLEMENTED
    - GetIdleTimeMinutes() / Update-IdleAwareState() using GetLastInputInfo API
    - Pause automatically after user idle > 30 minutes, resume on activity
    - Tray menu toggle with 'L' shortcut

14. **[x] Scheduled Operation** - IMPLEMENTED
    - Test-ScheduleActive() / Update-ScheduleState() functions
    - Auto-start at specific times (default: 9 AM weekdays)
    - Auto-stop at specific times (default: 6 PM weekdays)
    - Configurable via Redball.json

15. **[x] Presentation Mode Detection** - IMPLEMENTED
    - Test-PresentationMode() detects PowerPoint, Teams screenshare, Windows Presentation Mode
    - Auto-activate when presentation starts
    - Configurable via Redball.json

## Distribution & Trust

16. **[x] Code Signing - IMPLEMENTED**
    - Get-RedballCodeSigningCertificate() / Set-RedballCodeSignature() added
    - CLI signing support added: -SignScript -SignPath -CertThumbprint -TimestampServer
    - Uses Set-AuthenticodeSignature with timestamping and certificate selection

17. **[x] MSI Installer - IMPLEMENTED**
    - WiX installer source added: `installer/Redball.wxs`
    - Build script added: `installer/Build-MSI.ps1`
    - Includes Start Menu/Desktop shortcuts and .redball file association scaffolding

18. **[x] Auto-Updater - IMPLEMENTED**
    - Get-RedballLatestRelease() / Test-RedballUpdateAvailable() / Install-RedballUpdate() added
    - GitHub latest release check and version comparison against current script version
    - One-click script update with timestamped backup and optional restart
    - CLI switches added: -CheckUpdate and -Update

19. **[x] Digital Signature Verification - IMPLEMENTED**
    - Test-RedballFileSignature() function added
    - Verifies Authenticode status for downloaded files
    - Supports trusted signer thumbprint allowlist

## Developer Experience

20. **[x] Enhanced Test Coverage** - IMPLEMENTED
    - Added tests for Battery-Aware, Network-Aware, Idle Detection, CLI, Session State, Scheduled Operation, Presentation Mode

21. **[x] Performance Metrics** - IMPLEMENTED
    - Optional CPU/memory usage tracking
    - Logs every 5 minutes when enabled via `EnablePerformanceMetrics`

22. **[x] Telemetry (Opt-in) - IMPLEMENTED**
    - Send-RedballTelemetry() function added
    - Opt-in only via `EnableTelemetry` config flag
    - Session/event JSON logging for usage and state changes

23. **[x] Documentation** - IMPLEMENTED
    - Comment-based help added to ALL major functions
    - .SYNOPSIS, .DESCRIPTION, .PARAMETER, .EXAMPLE sections

## Security & Reliability

24. **[x] Execution Policy Handler** - IMPLEMENTED
    - Detects and guides user if policy blocks script
    - Provides helpful instructions to resolve

25. **[x] Backup/Restore Settings** - IMPLEMENTED
    - Export-RedballSettings() / Import-RedballSettings() functions
    - Supports optional base64 encryption
    - Exports config and state to JSON file

26. **[x] Crash Recovery** - IMPLEMENTED
    - Detects previous crash via flag file
    - Resets to safe defaults on restart after crash

27. **[x] Process Isolation - IMPLEMENTED**
    - Start-KeepAwakeRunspace() / Stop-KeepAwakeRunspace() added
    - Optional runspace-based keep-awake via `ProcessIsolation` config flag
    - Runspace lifecycle integrated into Set-ActiveState and Exit-Application

## Accessibility

28. **[x] Screen Reader Enhancements** - IMPLEMENTED
    - AccessibleName and AccessibleDescription on all menu items

29. **[x] High Contrast Mode** - IMPLEMENTED
    - Supports Windows high contrast themes
    - Accessible icon alternatives

30. **[x] Keyboard Navigation** - IMPLEMENTED
    - Keyboard shortcuts: Space, D, H, I, B, S, X
    - All tray menu items have keyboard access

## 10/10 Validation Check Results (2026-03-09, refreshed)

- ✅ MSI build smoke test passes (`installer/Build-MSI.ps1` creates `dist/Redball.msi`)
- ✅ JSON files validate (`Redball.json`, `locales.json`)
- ✅ Pester test suite now passing: **41/41 passed**
- ⚠️ PSScriptAnalyzer warnings reduced; no fatal errors
- ✅ Runtime config persistence restored (`Load-RedballConfig`/`Save-RedballConfig` added back and wired)
- ✅ CLI output polished (replaced `Write-Host` with host-safe output)
- ✅ Battery querying migrated to CIM (`Get-CimInstance`) from WMI cmdlets
- ✅ Process isolation runspace now self-initializes Win32 power API and falls back safely

## New Recommendations (Post-Validation)

31. **[x] Stabilize Pester test harness (HIGH PRIORITY)**
    - `Redball.Tests.ps1` is out of sync with current script/runtime setup
    - Missing function/context initialization causes widespread failures (`Get-RedballStatus`, schedule/presentation helpers)
    - Refactor tests to dot-source script safely and mock UI/timers consistently

32. **[x] Enforce failing exit code when tests fail (HIGH PRIORITY)**
    - Current Pester invocation can return exit code 0 even with failures
    - Use CI-safe invocation (`Invoke-Pester -CI` or assert `FailedCount -eq 0` and `exit 1`)

33. **[~] Triage and reduce PSScriptAnalyzer warnings (MEDIUM PRIORITY)**
    - Add targeted fixes/suppressions for:
      - `PSAvoidUsingEmptyCatchBlock`
      - `PSAvoidUsingWriteHost` (**resolved in core CLI and execution-policy messaging paths**)
      - `PSUseShouldProcessForStateChangingFunctions` (remaining; mostly intentional internal helper functions)
      - `PSAvoidUsingWMICmdlet` (**resolved for battery checks via CIM migration**)

34. **[ ] Add installer UI regression checks (MEDIUM PRIORITY)**
    - Validate generated `Redball-License.rtf` and icon assets before build
    - Add a quick script check to ensure WiX extension and required files are present

35. **[ ] Clean up roadmap consistency (LOW PRIORITY)**
    - Implementation Priority references `#23 Video Tutorial`, but item #23 is Documentation
    - Update priority list to align with current numbered items

## Implementation Priority

**High Priority (Do First):**
- #1 Toast Notifications
- #3 PowerShell 7 Compatibility
- #11 Battery-Aware Mode
- #20 Enhanced Test Coverage

**Medium Priority:**
- #2 Global Hotkeys
- #5 Settings GUI
- #7 Startup with Windows
- #12 Network-Aware

**Low Priority (Nice to Have):**
- #17 MSI Installer
- #18 Auto-Updater
- #23 Video Tutorial
- #27 Process Isolation

## Notes

- Current version: 2.0.0
- Last updated: 2026-03-09
- Most items are additive - won't break existing functionality
- Consider community contributions for lower priority items
