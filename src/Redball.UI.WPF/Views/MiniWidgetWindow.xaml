<Window x:Class="Redball.UI.Views.MiniWidgetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Redball Widget"
        Width="220" Height="80"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border Background="{DynamicResource SurfaceBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="10"
            Opacity="0.92">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.3" Color="Black"/>
        </Border.Effect>
        <Grid Margin="12,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Status dot -->
            <Ellipse x:Name="StatusDot" Grid.Column="0" Grid.RowSpan="2"
                     Width="12" Height="12" Fill="#4CAF50"
                     VerticalAlignment="Center" Margin="0,0,8,0"/>

            <!-- Title -->
            <TextBlock Grid.Column="1" Grid.Row="0"
                       Text="Redball" FontWeight="SemiBold" FontSize="12"
                       Foreground="{DynamicResource ForegroundBrush}"/>

            <!-- Status text -->
            <TextBlock x:Name="StatusText" Grid.Column="1" Grid.Row="1"
                       FontSize="10.5"
                       Foreground="{DynamicResource ForegroundSecondaryBrush}"
                       TextTrimming="CharacterEllipsis"/>

            <!-- Toggle button -->
            <Button x:Name="ToggleBtn" Grid.Column="2" Grid.RowSpan="2"
                    Content="&#xE769;" FontFamily="Segoe Fluent Icons" FontSize="16"
                    Background="Transparent" BorderThickness="0"
                    Foreground="{DynamicResource ForegroundSecondaryBrush}"
                    Width="28" Height="28" Padding="0"
                    VerticalAlignment="Center"
                    Click="ToggleBtn_Click" Cursor="Hand"
                    ToolTip="Toggle keep-awake"/>
        </Grid>
    </Border>
</Window>
