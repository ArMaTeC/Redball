<UserControl x:Class="Redball.UI.Views.HomeSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:lottie="clr-namespace:LottieSharp.WPF;assembly=LottieSharp"
             mc:Ignorable="d" d:DesignWidth="900" d:DesignHeight="700">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="0,0,0,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Row: TypeThing Stats + Keep-Awake + Settings -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <!-- TypeThing Stats Panel (large, red border) -->
                <Border Grid.Column="0" Style="{DynamicResource NeonRedPanelStyle}">
                    <DockPanel>
                        <!-- Header -->
                        <Grid DockPanel.Dock="Top" Margin="0,0,0,12">
                            <TextBlock Text="TypeThing Stats" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}"/>
                            <TextBlock Text="•••" HorizontalAlignment="Right" FontSize="14" Foreground="{DynamicResource ForegroundSecondaryBrush}" Cursor="Hand"/>
                        </Grid>

                        <!-- Stats Grid -->
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Line Chart Area (simulated with gradient path) -->
                            <Border Grid.Row="0" Grid.ColumnSpan="2" Background="#10FF003C" CornerRadius="8" Margin="0,0,0,12" Padding="12">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <!-- Mini stats -->
                                    <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left">
                                        <Border Background="#20FF003C" CornerRadius="4" Padding="6,3" Margin="0,0,8,0">
                                            <TextBlock Text="3,035" FontSize="11" Foreground="{DynamicResource AccentBrush}" FontWeight="SemiBold"/>
                                        </Border>
                                        <Border Background="#2000E5FF" CornerRadius="4" Padding="6,3">
                                            <TextBlock Text="2,057" FontSize="11" Foreground="{DynamicResource NeonCyanBrush}" FontWeight="SemiBold"/>
                                        </Border>
                                    </StackPanel>
                                    <!-- Visual chart representation -->
                                    <Canvas Grid.Row="1" Height="60">
                                        <!-- Red line -->
                                        <Polyline Points="0,45 30,30 60,35 90,20 120,25 150,15 180,30 210,20 240,28 270,10" 
                                                  Stroke="{DynamicResource AccentBrush}" StrokeThickness="2" Opacity="0.8"/>
                                        <!-- Cyan line -->
                                        <Polyline Points="0,50 30,40 60,42 90,35 120,38 150,30 180,38 210,32 240,35 270,22" 
                                                  Stroke="{DynamicResource NeonCyanBrush}" StrokeThickness="2" Opacity="0.6"/>
                                    </Canvas>
                                </Grid>
                            </Border>

                            <!-- Bottom stat cards -->
                            <Border Grid.Row="1" Grid.Column="0" Background="#10FF003C" CornerRadius="8" Padding="10" Margin="0,0,6,0">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <Ellipse Width="10" Height="10" Fill="{DynamicResource AccentBrush}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                        <TextBlock Text="66" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}"/>
                                    </StackPanel>
                                    <TextBlock Text="Sessions today" FontSize="10" Foreground="{DynamicResource ForegroundSecondaryBrush}" Margin="0,2,0,0"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Row="1" Grid.Column="1" Background="#1000E5FF" CornerRadius="8" Padding="10" Margin="6,0,0,0">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <Ellipse Width="10" Height="10" Fill="{DynamicResource NeonCyanBrush}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                        <TextBlock Text="50" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}"/>
                                    </StackPanel>
                                    <TextBlock Text="Chars/minute" FontSize="10" Foreground="{DynamicResource ForegroundSecondaryBrush}" Margin="0,2,0,0"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </DockPanel>
                </Border>

                <!-- Keep-Awake Panel (cyan border) -->
                <Border Grid.Column="1" Style="{DynamicResource NeonCyanPanelStyle}">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="Keep-Awake" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,16"/>
                        
                        <!-- Central Power Button -->
                        <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
                            <!-- Outer ring glow -->
                            <Ellipse Width="90" Height="90" StrokeThickness="3" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <Ellipse.Stroke>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#FF00E5FF" Offset="0"/>
                                        <GradientStop Color="#4000E5FF" Offset="0.5"/>
                                        <GradientStop Color="#FF00E5FF" Offset="1"/>
                                    </LinearGradientBrush>
                                </Ellipse.Stroke>
                                <Ellipse.Effect>
                                    <DropShadowEffect BlurRadius="15" ShadowDepth="0" Color="#FF00E5FF" Opacity="0.3"/>
                                </Ellipse.Effect>
                            </Ellipse>
                            <!-- Inner circle -->
                            <Ellipse Width="70" Height="70" Fill="#1800E5FF" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            <!-- Power icon -->
                            <TextBlock Text="&#xE7E8;" FontFamily="Segoe Fluent Icons" FontSize="28" Foreground="{DynamicResource NeonCyanBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>

                        <!-- Status with nav arrows at bottom -->
                        <Grid DockPanel.Dock="Bottom" Margin="0,12,0,0">
                            <TextBlock Text="{Binding StatusText}" FontSize="11" Foreground="{DynamicResource ForegroundSecondaryBrush}" HorizontalAlignment="Center"/>
                        </Grid>
                    </DockPanel>
                </Border>

                <!-- Settings Quick Panel (glass effect) -->
                <Border Grid.Column="2" Style="{DynamicResource GlassSettingsPanelStyle}">
                    <DockPanel>
                        <!-- Header with close icon -->
                        <Grid DockPanel.Dock="Top" Margin="0,0,0,12">
                            <TextBlock Text="Settings" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}"/>
                            <TextBlock Text="✕" HorizontalAlignment="Right" FontSize="12" Foreground="{DynamicResource ForegroundSecondaryBrush}" Cursor="Hand"/>
                        </Grid>

                        <StackPanel>
                            <!-- Toggle switches row -->
                            <Grid Margin="0,4,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Ellipse Width="8" Height="8" Fill="{DynamicResource AccentBrush}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                <ToggleButton Grid.Column="1" Style="{DynamicResource AnimatedToggleSwitch}" IsChecked="True"/>
                            </Grid>

                            <!-- Slider controls -->
                            <StackPanel Margin="0,4">
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <TextBlock Text="&#xE706;" FontFamily="Segoe Fluent Icons" FontSize="12" Foreground="{DynamicResource ForegroundSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <Border Background="{DynamicResource HoverBrush}" CornerRadius="2" Height="4" Width="120"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <TextBlock Text="&#xECAD;" FontFamily="Segoe Fluent Icons" FontSize="12" Foreground="{DynamicResource ForegroundSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <Border Background="{DynamicResource HoverBrush}" CornerRadius="2" Height="4" Width="120">
                                        <Border Background="{DynamicResource AccentBrush}" CornerRadius="2" Height="4" Width="50" HorizontalAlignment="Left"/>
                                    </Border>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <TextBlock Text="&#xE799;" FontFamily="Segoe Fluent Icons" FontSize="12" Foreground="{DynamicResource ForegroundSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <Border Background="{DynamicResource HoverBrush}" CornerRadius="2" Height="4" Width="120">
                                        <Border Background="{DynamicResource NeonCyanBrush}" CornerRadius="2" Height="4" Width="80" HorizontalAlignment="Left"/>
                                    </Border>
                                </StackPanel>
                            </StackPanel>

                            <!-- Icon buttons grid -->
                            <UniformGrid Columns="4" Margin="0,8,0,12">
                                <Border Background="#15FFFFFF" CornerRadius="8" Padding="8" Margin="2" Cursor="Hand">
                                    <TextBlock Text="&#xE912;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="{DynamicResource NeonCyanBrush}" HorizontalAlignment="Center"/>
                                </Border>
                                <Border Background="#15FFFFFF" CornerRadius="8" Padding="8" Margin="2" Cursor="Hand">
                                    <TextBlock Text="&#xE771;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="{DynamicResource ForegroundSecondaryBrush}" HorizontalAlignment="Center"/>
                                </Border>
                                <Border Background="#15FFFFFF" CornerRadius="8" Padding="8" Margin="2" Cursor="Hand">
                                    <TextBlock Text="&#xE9D9;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="{DynamicResource ForegroundSecondaryBrush}" HorizontalAlignment="Center"/>
                                </Border>
                                <Border Background="#15FFFFFF" CornerRadius="8" Padding="8" Margin="2" Cursor="Hand">
                                    <TextBlock Text="&#xE713;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="{DynamicResource ForegroundSecondaryBrush}" HorizontalAlignment="Center"/>
                                </Border>
                            </UniformGrid>

                            <!-- Menu items -->
                            <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Padding="0,8,0,0">
                                <StackPanel>
                                    <Grid Margin="0,4" Cursor="Hand">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="&#xE713;" FontFamily="Segoe Fluent Icons" FontSize="12" Foreground="{DynamicResource ForegroundSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                            <TextBlock Text="Configure" FontSize="12" Foreground="{DynamicResource ForegroundBrush}"/>
                                        </StackPanel>
                                        <TextBlock Text="›" HorizontalAlignment="Right" FontSize="14" Foreground="{DynamicResource ForegroundSecondaryBrush}"/>
                                    </Grid>
                                    <Grid Margin="0,4" Cursor="Hand">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="&#xE8C8;" FontFamily="Segoe Fluent Icons" FontSize="12" Foreground="{DynamicResource ForegroundSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                            <TextBlock Text="Menus" FontSize="12" Foreground="{DynamicResource ForegroundBrush}"/>
                                        </StackPanel>
                                        <TextBlock Text="›" HorizontalAlignment="Right" FontSize="14" Foreground="{DynamicResource ForegroundSecondaryBrush}"/>
                                    </Grid>
                                    <Grid Margin="0,4" Cursor="Hand">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="&#xE713;" FontFamily="Segoe Fluent Icons" FontSize="12" Foreground="{DynamicResource ForegroundSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                            <TextBlock Text="Settings" FontSize="12" Foreground="{DynamicResource ForegroundBrush}"/>
                                        </StackPanel>
                                        <TextBlock Text="›" HorizontalAlignment="Right" FontSize="14" Foreground="{DynamicResource ForegroundSecondaryBrush}"/>
                                    </Grid>
                                    <Grid Margin="0,4" Cursor="Hand">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="&#xE8A5;" FontFamily="Segoe Fluent Icons" FontSize="12" Foreground="{DynamicResource ForegroundSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                            <TextBlock Text="Log" FontSize="12" Foreground="{DynamicResource ForegroundBrush}"/>
                                        </StackPanel>
                                        <TextBlock Text="›" HorizontalAlignment="Right" FontSize="14" Foreground="{DynamicResource ForegroundSecondaryBrush}"/>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </DockPanel>
                </Border>
            </Grid>

            <!-- Bottom Row: Activity Monitor + Quick Actions -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <!-- Activity Monitor Panel -->
                <Border Grid.Column="0" Style="{DynamicResource CardStyle}">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="Activity Monitor" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,12"/>
                        
                        <!-- Chart area with dual lines -->
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Canvas Grid.Row="0" Height="80" Margin="0,0,0,8">
                                <!-- Red activity line -->
                                <Polyline Points="0,60 40,50 80,55 120,30 160,45 200,25 240,40 280,20 320,35 360,15 400,30" 
                                          Stroke="{DynamicResource AccentBrush}" StrokeThickness="2" Opacity="0.7"/>
                                <!-- Cyan usage line -->
                                <Polyline Points="0,65 40,55 80,60 120,40 160,50 200,35 240,45 280,30 320,40 360,25 400,38" 
                                          Stroke="{DynamicResource NeonCyanBrush}" StrokeThickness="2" Opacity="0.5"/>
                                <!-- Grid lines -->
                                <Line X1="0" Y1="20" X2="400" Y2="20" Stroke="{DynamicResource BorderBrush}" StrokeThickness="0.5"/>
                                <Line X1="0" Y1="40" X2="400" Y2="40" Stroke="{DynamicResource BorderBrush}" StrokeThickness="0.5"/>
                                <Line X1="0" Y1="60" X2="400" Y2="60" Stroke="{DynamicResource BorderBrush}" StrokeThickness="0.5"/>
                            </Canvas>

                            <!-- Legend -->
                            <StackPanel Grid.Row="1" Orientation="Horizontal">
                                <StackPanel Orientation="Horizontal" Margin="0,0,16,0">
                                    <Ellipse Width="8" Height="8" Fill="{DynamicResource AccentBrush}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Keep-Awake" FontSize="10" Foreground="{DynamicResource ForegroundSecondaryBrush}" VerticalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="8" Height="8" Fill="{DynamicResource NeonCyanBrush}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="TypeThing" FontSize="10" Foreground="{DynamicResource ForegroundSecondaryBrush}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </DockPanel>
                </Border>

                <!-- Quick Actions Panel -->
                <Border Grid.Column="1" Style="{DynamicResource CardStyle}">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="Quick Actions" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,12"/>
                        
                        <StackPanel>
                            <!-- Action items with neon indicators -->
                            <Border Background="#10FF003C" CornerRadius="8" Padding="10" Margin="0,0,0,6" Cursor="Hand">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Width="6" Height="30" CornerRadius="3" Background="{DynamicResource AccentBrush}" Margin="0,0,10,0"/>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="Toggle Engine" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}"/>
                                        <TextBlock Text="Keep-Awake control" FontSize="10" Foreground="{DynamicResource ForegroundSecondaryBrush}"/>
                                    </StackPanel>
                                    <Button Grid.Column="2" Content="Toggle" Command="{Binding ToggleActiveCommand}" Style="{DynamicResource AnimatedPrimaryButton}" Padding="10,4" FontSize="11" MinHeight="24"/>
                                </Grid>
                            </Border>

                            <Border Background="#1000E5FF" CornerRadius="8" Padding="10" Margin="0,0,0,6" Cursor="Hand">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Width="6" Height="30" CornerRadius="3" Background="{DynamicResource NeonCyanBrush}" Margin="0,0,10,0"/>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="TypeThing" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}"/>
                                        <TextBlock Text="Launch clipboard typing" FontSize="10" Foreground="{DynamicResource ForegroundSecondaryBrush}"/>
                                    </StackPanel>
                                    <Button Grid.Column="2" Content="Launch" Command="{Binding OpenTypeThingCommand}" Style="{DynamicResource AnimatedModernButton}" Padding="10,4" FontSize="11" MinHeight="24"/>
                                </Grid>
                            </Border>

                            <Border Background="#10FFFFFF" CornerRadius="8" Padding="10" Margin="0,0,0,6" Cursor="Hand">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Width="6" Height="30" CornerRadius="3" Background="{DynamicResource ForegroundSecondaryBrush}" Margin="0,0,10,0"/>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="View Logs" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}"/>
                                        <TextBlock Text="System diagnostics" FontSize="10" Foreground="{DynamicResource ForegroundSecondaryBrush}"/>
                                    </StackPanel>
                                    <Button Grid.Column="2" Content="Open" Command="{Binding OpenLogsCommand}" Style="{DynamicResource AnimatedModernButton}" Padding="10,4" FontSize="11" MinHeight="24"/>
                                </Grid>
                            </Border>

                            <!-- Configuration row -->
                            <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Margin="0,6,0,0" Padding="0,8,0,0">
                                <Grid>
                                    <StackPanel>
                                        <TextBlock Text="Configuration &amp; Personalization" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}"/>
                                        <TextBlock Text="Profiles, themes, and updates" FontSize="10" Foreground="{DynamicResource ForegroundSecondaryBrush}" Margin="0,2,0,0"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                        <Button Content="Settings" Command="{Binding OpenSettingsCommand}" Style="{DynamicResource AnimatedModernButton}" Padding="8,3" FontSize="10" MinHeight="22" Margin="0,0,4,0"/>
                                        <Button Content="Updates" Command="{Binding CheckForUpdatesCommand}" Style="{DynamicResource AnimatedModernButton}" Padding="8,3" FontSize="10" MinHeight="22"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </DockPanel>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
