<UserControl x:Class="Redball.UI.Views.CommandPaletteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Redball.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Visibility="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
    <Grid Background="{DynamicResource BackgroundBrush}" Opacity="0.9" MouseDown="Overlay_MouseDown">
        <Border Width="500" VerticalAlignment="Top" Margin="0,100,0,0"
                Background="{DynamicResource SurfaceBrush}"
                BorderThickness="2"
                CornerRadius="12"
                Effect="{DynamicResource ShadowEffect}">
            <Border.BorderBrush>
                <LinearGradientBrush x:Name="PaletteBorderBrush" StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="{DynamicResource AccentColor}" Offset="0"/>
                    <GradientStop Color="{DynamicResource BorderLightBrush}" Offset="0.5"/>
                    <GradientStop Color="{DynamicResource AccentColor}" Offset="1"/>
                </LinearGradientBrush>
            </Border.BorderBrush>
            <Border.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard RepeatBehavior="Forever">
                            <PointAnimation Storyboard.TargetName="PaletteBorderBrush" 
                                            Storyboard.TargetProperty="StartPoint"
                                            From="0,0" To="1,1" Duration="0:0:3" AutoReverse="True"/>
                            <PointAnimation Storyboard.TargetName="PaletteBorderBrush" 
                                            Storyboard.TargetProperty="EndPoint"
                                            From="1,1" To="0,0" Duration="0:0:3" AutoReverse="True"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Border.Triggers>
            <Grid Margin="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="&#xE721;" FontFamily="Segoe Fluent Icons" FontSize="16" 
                               Margin="8,0,12,0" VerticalAlignment="Center" 
                               Foreground="{DynamicResource ForegroundSecondaryBrush}"/>
                    <TextBox x:Name="SearchBox" Grid.Column="1" 
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Style="{DynamicResource ModernTextBox}"
                             Tag="Search commands..." 
                             BorderThickness="0"
                             FontSize="14"
                             PreviewKeyDown="SearchBox_PreviewKeyDown"/>
                </Grid>

                <Separator Grid.Row="1" Background="{DynamicResource BorderBrush}" Margin="-12,0,-12,12"/>

                <ListBox Grid.Row="2" ItemsSource="{Binding FilteredCommands}"
                         SelectedItem="{Binding SelectedCommand}"
                         Background="Transparent" BorderThickness="0"
                         MaxHeight="300" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="4,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="40"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Icon}" FontFamily="Segoe Fluent Icons" 
                                           FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"
                                           Foreground="{DynamicResource AccentBrush}"/>
                                <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13"/>
                                    <TextBlock Text="{Binding Description}" FontSize="11" 
                                               Foreground="{DynamicResource ForegroundSecondaryBrush}"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border Name="Border" Background="{TemplateBinding Background}" 
                                                CornerRadius="4" Margin="2,1">
                                            <ContentPresenter />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource HoverBrush}"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource HoverBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</UserControl>
