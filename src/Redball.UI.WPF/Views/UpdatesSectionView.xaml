<UserControl x:Class="Redball.UI.Views.UpdatesSectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="StageIndicator" TargetType="Border">
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Background" Value="#33555555"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style x:Key="StageLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="9"/>
            <Setter Property="Foreground" Value="#77AAAAAA"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
        </Style>
        <Style x:Key="StageConnector" TargetType="Border">
            <Setter Property="Height" Value="2"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Background" Value="#33555555"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <TextBlock Text="Updates" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBlock Text="Manage how Redball checks for and installs updates. Choose between stable releases, beta versions, or disable automatic updates entirely." TextWrapping="Wrap" FontSize="12" Foreground="{DynamicResource ForegroundSecondaryBrush}" Margin="0,0,0,16"/>

        <Border x:Name="SettingsPanel" Style="{DynamicResource CardStyle}">
            <StackPanel>
                <TextBlock Text="Update Settings" FontWeight="Medium" Margin="0,0,0,8"/>
                <TextBlock Text="Update Channel" FontWeight="Medium" Margin="0,0,0,4"/>
                <ComboBox x:Name="UpdateChannelCombo" Style="{DynamicResource ModernComboBox}" Margin="0,0,0,16" SelectionChanged="UpdateChannelCombo_SelectionChanged">
                    <ComboBoxItem Content="Stable (Recommended)"/>
                    <ComboBoxItem Content="Beta"/>
                    <ComboBoxItem Content="Disabled"/>
                </ComboBox>
                <CheckBox x:Name="VerifyUpdateSignatureCheck" Content="Verify update signatures" Style="{DynamicResource ModernCheckBox}" Margin="0,0,0,8" Checked="VerifyUpdateSignatureCheck_Changed" Unchecked="VerifyUpdateSignatureCheck_Changed"/>
                <TextBlock Text="Only install signed updates from trusted sources" FontSize="12" Foreground="{DynamicResource ForegroundSecondaryBrush}" Margin="24,0,0,16"/>
                <StackPanel x:Name="AutoUpdatePanel" Margin="0,0,0,16">
                    <CheckBox x:Name="AutoUpdateCheckEnabledCheck" Content="Check for updates automatically" Style="{DynamicResource ModernCheckBox}" Margin="0,0,0,8" Checked="AutoUpdateCheckEnabledCheck_Changed" Unchecked="AutoUpdateCheckEnabledCheck_Changed"/>
                    <TextBlock Text="Auto-check Interval (minutes)" FontWeight="Medium" Margin="0,0,0,4" Foreground="{DynamicResource ForegroundBrush}"/>
                    <Slider x:Name="AutoUpdateIntervalSlider" Minimum="30" Maximum="1440" TickFrequency="30" IsSnapToTickEnabled="True" Style="{DynamicResource ModernSlider}" ValueChanged="AutoUpdateIntervalSlider_ValueChanged"/>
                    <TextBlock x:Name="AutoUpdateIntervalText" FontSize="12" Foreground="{DynamicResource ForegroundSecondaryBrush}" Margin="0,4,0,0"/>
                </StackPanel>
                <TextBlock x:Name="CurrentVersionText" FontSize="12" Foreground="{DynamicResource ForegroundSecondaryBrush}" Margin="0,0,0,8"/>
                <Button x:Name="CheckForUpdatesButton" Content="Check for Updates Now" Style="{DynamicResource PrimaryButton}" HorizontalAlignment="Left" Click="CheckForUpdatesButton_Click"/>
            </StackPanel>
        </Border>

        <Border x:Name="ProgressPanel" Style="{DynamicResource CardStyle}" Visibility="Collapsed">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                    <TextBlock Text="&#xE895;" FontFamily="Segoe Fluent Icons" Foreground="{DynamicResource AccentBrush}" FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock x:Name="ProgressTitleText" Text="Checking for Updates" Foreground="{DynamicResource ForegroundBrush}" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <TextBlock x:Name="ProgressPercentText" Text="" Foreground="{DynamicResource AccentBrush}" FontSize="14" FontWeight="Medium" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,12">
                    <StackPanel>
                        <Border x:Name="StageChecking" Style="{StaticResource StageIndicator}">
                            <TextBlock Text="&#xE8C8;" FontFamily="Segoe Fluent Icons" FontSize="11" Foreground="#77AAAAAA" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Style="{StaticResource StageLabel}" Text="Check"/>
                    </StackPanel>
                    <Border x:Name="Connector1" Style="{StaticResource StageConnector}"/>
                    <StackPanel>
                        <Border x:Name="StageDownloading" Style="{StaticResource StageIndicator}">
                            <TextBlock Text="&#xE896;" FontFamily="Segoe Fluent Icons" FontSize="11" Foreground="#77AAAAAA" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Style="{StaticResource StageLabel}" Text="Download"/>
                    </StackPanel>
                    <Border x:Name="Connector2" Style="{StaticResource StageConnector}"/>
                    <StackPanel>
                        <Border x:Name="StagePatching" Style="{StaticResource StageIndicator}">
                            <TextBlock Text="&#xE90F;" FontFamily="Segoe Fluent Icons" FontSize="11" Foreground="#77AAAAAA" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Style="{StaticResource StageLabel}" Text="Patch"/>
                    </StackPanel>
                    <Border x:Name="Connector3" Style="{StaticResource StageConnector}"/>
                    <StackPanel>
                        <Border x:Name="StageVerifying" Style="{StaticResource StageIndicator}">
                            <TextBlock Text="&#xE8FB;" FontFamily="Segoe Fluent Icons" FontSize="11" Foreground="#77AAAAAA" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Style="{StaticResource StageLabel}" Text="Verify"/>
                    </StackPanel>
                    <Border x:Name="Connector4" Style="{StaticResource StageConnector}"/>
                    <StackPanel>
                        <Border x:Name="StageApplying" Style="{StaticResource StageIndicator}">
                            <TextBlock Text="&#xE74B;" FontFamily="Segoe Fluent Icons" FontSize="11" Foreground="#77AAAAAA" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Style="{StaticResource StageLabel}" Text="Apply"/>
                    </StackPanel>
                </StackPanel>

                <TextBlock x:Name="ProgressStatusText" Text="Preparing update..." Foreground="{DynamicResource ForegroundBrush}" FontSize="13" FontWeight="Medium" Margin="0,0,0,8" TextTrimming="CharacterEllipsis"/>

                <Grid Margin="0,0,0,4">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <ProgressBar x:Name="ProgressBar" Height="8" Minimum="0" Maximum="100" Value="0" Background="#22555555" Foreground="{DynamicResource AccentBrush}" BorderThickness="0">
                        <ProgressBar.Template>
                            <ControlTemplate TargetType="ProgressBar">
                                <Grid x:Name="TemplateRoot">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="4"/>
                                    <Border x:Name="PART_Indicator" HorizontalAlignment="Left" CornerRadius="4" Background="{TemplateBinding Foreground}"/>
                                </Grid>
                            </ControlTemplate>
                        </ProgressBar.Template>
                    </ProgressBar>
                    <Grid Grid.Row="1" Margin="0,5,0,0">
                        <TextBlock x:Name="ProgressSizeText" Text="" Foreground="{DynamicResource ForegroundSecondaryBrush}" FontSize="10.5" HorizontalAlignment="Left"/>
                        <TextBlock x:Name="ProgressSpeedText" Text="" Foreground="{DynamicResource AccentBrush}" FontSize="10.5" FontWeight="Medium" HorizontalAlignment="Right"/>
                    </Grid>
                </Grid>

                <Border Background="#11FFFFFF" CornerRadius="8" Margin="0,8,0,0" Padding="0" MaxHeight="150">
                    <ScrollViewer x:Name="LogScrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="10,8">
                        <TextBlock x:Name="LogTextBlock" Foreground="#AACCCCCC" FontFamily="Cascadia Mono,Consolas,Courier New" FontSize="10.5" TextWrapping="Wrap" LineHeight="17"/>
                    </ScrollViewer>
                </Border>

                <Grid Margin="0,6,0,0">
                    <TextBlock x:Name="ProgressEtaText" Text="" Foreground="{DynamicResource ForegroundSecondaryBrush}" FontSize="10" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    <TextBlock x:Name="ProgressFileCountText" Text="" Foreground="{DynamicResource ForegroundSecondaryBrush}" FontSize="10" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                </Grid>

                <Button x:Name="CancelUpdateButton" Content="Cancel" Style="{DynamicResource ModernButton}" HorizontalAlignment="Right" Margin="0,12,0,0" Click="CancelUpdateButton_Click"/>
            </StackPanel>
        </Border>

        <Border x:Name="ResultPanel" Style="{DynamicResource CardStyle}" Visibility="Collapsed">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                    <TextBlock x:Name="ResultIcon" Text="&#xE73E;" FontFamily="Segoe Fluent Icons" Foreground="{DynamicResource SuccessBrush}" FontSize="28" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock x:Name="ResultTitleText" Text="Up to Date" Foreground="{DynamicResource ForegroundBrush}" FontSize="18" FontWeight="SemiBold"/>
                        <TextBlock x:Name="ResultMessageText" Text="You're running the latest version of Redball." Foreground="{DynamicResource ForegroundSecondaryBrush}" FontSize="12" TextWrapping="Wrap"/>
                    </StackPanel>
                </StackPanel>

                <!-- Update Available Details Panel -->
                <StackPanel x:Name="UpdateAvailablePanel" Visibility="Collapsed">
                    <!-- Version Comparison Card -->
                    <Border Background="#15FFFFFF" CornerRadius="8" Padding="12" Margin="0,0,0,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                                <TextBlock Text="Current" FontSize="11" Foreground="{DynamicResource ForegroundSecondaryBrush}" Margin="0,0,0,4"/>
                                <TextBlock x:Name="CurrentVersionCompareText" Text="v2.1.500" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}"/>
                            </StackPanel>
                            <TextBlock Grid.Column="1" Text="&#xE72A;" FontFamily="Segoe Fluent Icons" FontSize="20" Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Center" Margin="16,0"/>
                            <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                                <TextBlock Text="New" FontSize="11" Foreground="{DynamicResource ForegroundSecondaryBrush}" Margin="0,0,0,4"/>
                                <TextBlock x:Name="NewVersionCompareText" Text="v2.1.501" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource SuccessBrush}"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Update Details -->
                    <StackPanel Margin="0,0,0,12">
                        <TextBlock x:Name="UpdateSizeText" Text="Download size: Calculating..." FontSize="12" Foreground="{DynamicResource ForegroundSecondaryBrush}" Margin="0,0,0,4"/>
                        <TextBlock x:Name="UpdateTypeText" Text="Update type: Differential" FontSize="12" Foreground="{DynamicResource ForegroundSecondaryBrush}" Margin="0,0,0,4"/>
                    </StackPanel>

                    <!-- Release Notes Section -->
                    <Border Background="#10FFFFFF" CornerRadius="8" Padding="12" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="Release Notes" FontWeight="SemiBold" FontSize="13" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,8"/>
                            <ScrollViewer MaxHeight="150" VerticalScrollBarVisibility="Auto">
                                <TextBlock x:Name="ReleaseNotesText" Text="No release notes available." TextWrapping="Wrap" FontSize="12" Foreground="{DynamicResource ForegroundSecondaryBrush}" LineHeight="18"/>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>

                    <!-- Action Buttons -->
                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="DownloadUpdateButton" Content="Download &amp; Install" Style="{DynamicResource PrimaryButton}" Click="DownloadUpdateButton_Click" Padding="20,8"/>
                        <Button x:Name="DismissUpdateButton" Content="Remind Me Later" Style="{DynamicResource ModernButton}" Margin="12,0,0,0" Click="DismissUpdateButton_Click" Padding="16,8"/>
                    </StackPanel>
                </StackPanel>

                <!-- Back Button (shown for all results) -->
                <Button x:Name="BackToSettingsButton" Content="Back to Settings" Style="{DynamicResource ModernButton}" HorizontalAlignment="Left" Margin="0,16,0,0" Click="BackToSettingsButton_Click"/>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
