<UserControl x:Class="Redball.UI.Views.UpdatesSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBlock Text="Updates" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBlock Text="Manage how Redball checks for and installs updates. Choose between stable releases, beta versions, or disable automatic updates entirely." 
                   TextWrapping="Wrap" FontSize="12" Foreground="{DynamicResource ForegroundSecondaryBrush}" Margin="0,0,0,16"/>
        
        <Border Style="{DynamicResource CardStyle}">
            <StackPanel>
                <TextBlock Text="Update Channel" FontWeight="Medium" Margin="0,0,0,8"/>
                <ComboBox x:Name="UpdateChannelCombo" Style="{DynamicResource ModernComboBox}" Margin="0,0,0,16">
                    <ComboBoxItem Content="Stable (Recommended)"/>
                    <ComboBoxItem Content="Beta"/>
                    <ComboBoxItem Content="Disabled"/>
                </ComboBox>
                <CheckBox x:Name="VerifyUpdateSignatureCheck" Content="Verify update signatures" Style="{DynamicResource ModernCheckBox}" Margin="0,0,0,8"/>
                <TextBlock Text="Only install signed updates from trusted sources" FontSize="12" Foreground="{DynamicResource ForegroundSecondaryBrush}" Margin="24,0,0,16"/>
                <Button Content="Check for Updates Now" Style="{DynamicResource PrimaryButton}" HorizontalAlignment="Left"/>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
