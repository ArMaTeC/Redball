<Window x:Class="Redball.UI.Views.HUDWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="HUD" 
        Height="120" 
        Width="300"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterScreen"
        IsHitTestVisible="False">

    <Window.Resources>
        <Storyboard x:Key="ShowHideStoryboard">
            <DoubleAnimation Storyboard.TargetName="MainBorder" 
                             Storyboard.TargetProperty="Opacity"
                             From="0" To="0.95" Duration="0:0:0.2"/>
            <DoubleAnimation Storyboard.TargetName="MainBorder" 
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                             From="0.8" To="1.0" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <BackEase Amplitude="0.5" EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="MainBorder" 
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                             From="0.8" To="1.0" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <BackEase Amplitude="0.5" EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            
            <!-- Stay visible for 1.5s -->
            <DoubleAnimation Storyboard.TargetName="MainBorder" 
                             Storyboard.TargetProperty="Opacity"
                             From="0.95" To="0" BeginTime="0:0:1.7" Duration="0:0:0.4"/>
        </Storyboard>
    </Window.Resources>

    <Border x:Name="MainBorder" 
            Margin="10" 
            CornerRadius="20" 
            Background="#CC1E1E1E" 
            BorderBrush="#44FFFFFF" 
            BorderThickness="1"
            Opacity="0"
            RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <ScaleTransform ScaleX="0.8" ScaleY="0.8"/>
        </Border.RenderTransform>
        <Border.Effect>
            <BlurEffect Radius="0" />
        </Border.Effect>

        <Grid Margin="20,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Viewbox Grid.Column="0" Width="40" Height="40" Margin="0,0,15,0">
                <TextBlock x:Name="IconText" Text="🔔" FontSize="30" Foreground="White" VerticalAlignment="Center"/>
            </Viewbox>

            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock x:Name="TitleText" 
                           Text="KEEP-AWAKE" 
                           Foreground="#88FFFFFF" 
                           FontSize="12" 
                           FontWeight="SemiBold"/>
                <TextBlock x:Name="StatusText" 
                           Text="ACTIVATED" 
                           Foreground="White" 
                           FontSize="20" 
                           FontWeight="Bold"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
