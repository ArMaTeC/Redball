<Window x:Class="Redball.UI.Views.UpdateProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Downloading Update" 
        Height="180" Width="440"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True">
    
    <Border BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1.5"
            CornerRadius="16"
            ClipToBounds="True">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FB202020" Offset="0"/>
                <GradientStop Color="#F02D2D2D" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.6" Color="Black"/>
        </Border.Effect>

        <Grid Margin="24,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                <TextBlock Text="&#xE896;" FontFamily="Segoe MDL2 Assets" 
                           Foreground="{DynamicResource AccentBrush}" 
                           FontSize="18" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock x:Name="StatusLabel" Text="Downloading update..." 
                           Foreground="{DynamicResource ForegroundBrush}" FontSize="15" FontWeight="SemiBold"
                           VerticalAlignment="Center"/>
            </StackPanel>

            <Grid Grid.Row="1" Margin="0,0,0,8">
                <TextBlock x:Name="SizeLabel" Text="0 MB / 0 MB" 
                           Foreground="{DynamicResource ForegroundSecondaryBrush}" FontSize="11" HorizontalAlignment="Left"/>
                <TextBlock x:Name="SpeedLabel" Text="0 KB/s" 
                           Foreground="{DynamicResource AccentBrush}" FontSize="11" FontWeight="Medium" 
                           HorizontalAlignment="Right"/>
            </Grid>
            
            <ProgressBar x:Name="ProgressBar"
                         Grid.Row="2"
                         Height="10"
                         VerticalAlignment="Top"
                         Minimum="0"
                         Maximum="100"
                         Value="0"
                         Background="{DynamicResource HoverBrush}"
                         Foreground="{DynamicResource AccentBrush}"
                         BorderThickness="0">
                <ProgressBar.Template>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid x:Name="TemplateRoot">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Determinate"/>
                                    <VisualState x:Name="Indeterminate"/>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Border Background="{TemplateBinding Background}" CornerRadius="5"/>
                            <Border x:Name="PART_Indicator" HorizontalAlignment="Left" CornerRadius="5" Background="{TemplateBinding Foreground}">
                                <Border.Effect>
                                    <DropShadowEffect Color="{Binding Color, Source={StaticResource AccentBrush}}" BlurRadius="10" ShadowDepth="0" Opacity="0.6"/>
                                </Border.Effect>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </ProgressBar.Template>
            </ProgressBar>

            <TextBlock x:Name="EtaLabel" Grid.Row="3" 
                       Text="Calculating remaining time..." 
                       Foreground="{DynamicResource ForegroundSecondaryBrush}" FontSize="10" HorizontalAlignment="Center" Margin="0,4,0,0"/>
        </Grid>
    </Border>
</Window>
