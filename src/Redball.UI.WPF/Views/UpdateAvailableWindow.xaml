<Window x:Class="Redball.UI.Views.UpdateAvailableWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update Available" 
        Height="560" Width="520"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/MainWindowStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border CornerRadius="12"
            Background="{DynamicResource BackgroundBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="16" ShadowDepth="0" Color="#55000000" Opacity="0.4"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}" CornerRadius="12,12,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="20,14,20,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="&#xE896;" FontFamily="Segoe Fluent Icons" FontSize="22"
                               Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <TextBlock Grid.Column="1" Text="Update Available" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <Button Grid.Column="2" Content="✕" Click="CloseButton_Click"
                            Background="Transparent" BorderThickness="0" Foreground="{DynamicResource ForegroundSecondaryBrush}"
                            FontSize="14" Cursor="Hand" Padding="6,2"/>
                </Grid>
            </Border>

            <!-- Version summary -->
            <Border Grid.Row="1" Margin="20,16,20,0">
                <StackPanel>
                    <TextBlock x:Name="VersionSummaryText" FontSize="14" FontWeight="Medium" TextWrapping="Wrap"/>
                    <TextBlock x:Name="VersionCountText" FontSize="12" Foreground="{DynamicResource ForegroundSecondaryBrush}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Changelog list (scrollable) -->
            <ScrollViewer Grid.Row="2" Margin="20,12,20,0" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="ChangelogPanel"/>
            </ScrollViewer>

            <!-- Action buttons -->
            <Border Grid.Row="3" Background="{DynamicResource SurfaceBrush}" CornerRadius="0,0,12,12"
                    Margin="0,12,0,0">
                <Grid Margin="20,14,20,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <CheckBox x:Name="DontRemindCheck" Grid.Column="0" Content="Don't remind me for this version"
                              Foreground="{DynamicResource ForegroundSecondaryBrush}" FontSize="11" VerticalAlignment="Center"/>
                    <Button Grid.Column="2" Content="Remind Me Later" Click="RemindLaterButton_Click"
                            Style="{DynamicResource ModernButton}" Padding="16,8" Margin="0,0,8,0"/>
                    <Button Grid.Column="3" Content="Update Now" Click="UpdateNowButton_Click"
                            Style="{DynamicResource PrimaryButton}" Padding="16,8"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
