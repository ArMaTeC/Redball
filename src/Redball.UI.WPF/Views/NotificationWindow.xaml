<Window x:Class="Redball.UI.Views.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Notification" 
        Height="Auto" Width="400"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False">
    
    <Border BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="12"
            Background="{DynamicResource BackgroundBrush}"
            Margin="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="0" Opacity="0.5" Color="Black"/>
        </Border.Effect>

        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                <TextBlock x:Name="IconText" Text="&#xE946;" 
                           FontFamily="Segoe MDL2 Assets" 
                           Foreground="{DynamicResource AccentBrush}" 
                           FontSize="24" VerticalAlignment="Center" Margin="0,0,16,0"/>
                <TextBlock x:Name="TitleLabel" Text="Notice" 
                           Foreground="{DynamicResource ForegroundBrush}" 
                           FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>

            <TextBlock x:Name="MessageLabel" Grid.Row="1" 
                       Text="Message goes here..." 
                       Foreground="{DynamicResource ForegroundSecondaryBrush}" 
                       FontSize="13" TextWrapping="Wrap" Margin="0,0,0,24"/>

            <StackPanel x:Name="ButtonPanel" Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CancelButton" Content="Cancel" 
                        Width="85" Height="32" 
                        Margin="0,0,8,0"
                        Visibility="Collapsed"
                        Click="CancelButton_Click"
                        Style="{DynamicResource ModernButton}"/>
                <Button x:Name="OkButton" Content="OK" 
                        Width="85" Height="32" 
                        Click="OkButton_Click"
                        Style="{DynamicResource PrimaryButton}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
