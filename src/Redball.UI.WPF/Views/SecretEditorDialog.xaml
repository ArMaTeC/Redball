<Window x:Class="Redball.UI.Views.SecretEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Secret" Height="250" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Secret Key:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" x:Name="KeyTextBox" Margin="0,0,0,15"/>
        
        <TextBlock Grid.Row="2" Text="Secret Value:" Margin="0,0,0,5"/>
        <PasswordBox Grid.Row="3" x:Name="ValuePasswordBox" Margin="0,0,0,20"/>
        
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SaveButton" Content="Save" Click="Save_Click" Padding="20,5" Margin="0,0,10,0" IsDefault="True"/>
            <Button x:Name="CancelButton" Content="Cancel" Click="Cancel_Click" Padding="20,5" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
