<UserControl x:Class="Redball.UI.Views.SmartFeaturesSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBlock Text="Smart Features" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBlock Text="Intelligent automation that adapts Redball to your situation. Pause keep-awake when your battery is low, when you lose network connectivity, or when you've been idle." 
                   TextWrapping="Wrap" FontSize="12" Foreground="{DynamicResource ForegroundSecondaryBrush}" Margin="0,0,0,16"/>
        
        <Border Style="{DynamicResource CardStyle}">
            <StackPanel>
                <CheckBox x:Name="BatteryAwareCheck" Content="Battery-aware mode" Style="{DynamicResource ModernCheckBox}" Margin="0,0,0,8"/>
                <TextBlock Text="Pause when battery is below threshold" FontSize="12" Foreground="{DynamicResource ForegroundSecondaryBrush}" Margin="24,0,0,8"/>
                <Slider x:Name="BatteryThresholdSlider" Minimum="5" Maximum="50" Value="20" Style="{DynamicResource ModernSlider}" Margin="24,0,0,8"/>
                <TextBlock x:Name="BatteryThresholdText" Text="Threshold: 20%" FontSize="12" Margin="24,0,0,16"/>
                <CheckBox x:Name="NetworkAwareCheck" Content="Network-aware mode" Style="{DynamicResource ModernCheckBox}" Margin="0,0,0,8"/>
                <CheckBox x:Name="IdleDetectionCheck" Content="Idle detection" Style="{DynamicResource ModernCheckBox}" Margin="0,0,0,8"/>
                <Slider x:Name="IdleThresholdSlider" Minimum="5" Maximum="120" Value="30" Style="{DynamicResource ModernSlider}" Margin="24,0,0,8"/>
                <TextBlock x:Name="IdleThresholdText" Text="Threshold: 30 minutes" FontSize="12" Margin="24,0,0,0"/>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
