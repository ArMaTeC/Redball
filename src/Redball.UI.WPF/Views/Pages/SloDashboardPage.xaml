<Page x:Class="Redball.UI.WPF.Views.Pages.SloDashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      mc:Ignorable="d"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      d:DesignHeight="600" d:DesignWidth="800"
      Loaded="Page_Loaded" Unloaded="Page_Unloaded"
      Title="SLO Dashboard">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Performance SLO Dashboard" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Health Status -->
        <Border x:Name="HealthStatusPanel" Grid.Row="1" Background="LightGreen" CornerRadius="8" Padding="15" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="HealthStatusIcon" Text="\uE8FB" FontFamily="Segoe MDL2 Assets" FontSize="24" Margin="0,0,10,0"/>
                <TextBlock x:Name="HealthStatusText" Text="Healthy" FontSize="16" FontWeight="Semibold"/>
            </StackPanel>
        </Border>

        <!-- SLO Targets -->
        <UniformGrid Grid.Row="2" Columns="2" Margin="0,0,0,20">
            <Border Background="WhiteSmoke" CornerRadius="8" Padding="15" Margin="5">
                <StackPanel>
                    <TextBlock Text="Cold Start SLO" FontSize="12" Foreground="Gray"/>
                    <TextBlock x:Name="ColdStartTargetText" Text="1.5s" FontSize="28" FontWeight="Bold"/>
                    <TextBlock Text="Target: 95% pass rate" FontSize="11" Foreground="Gray"/>
                </StackPanel>
            </Border>

            <Border Background="WhiteSmoke" CornerRadius="8" Padding="15" Margin="5">
                <StackPanel>
                    <TextBlock Text="Warm Start SLO" FontSize="12" Foreground="Gray"/>
                    <TextBlock x:Name="WarmStartTargetText" Text="0.8s" FontSize="28" FontWeight="Bold"/>
                    <TextBlock Text="Target: 99% pass rate" FontSize="11" Foreground="Gray"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Statistics -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="5">
                <TextBlock Text="7-Day Statistics" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>

                <TextBlock Text="Total Starts" Foreground="Gray"/>
                <TextBlock x:Name="TotalStartsText" Text="0" FontSize="20" Margin="0,0,0,10"/>

                <TextBlock Text="Cold Start Pass Rate" Foreground="Gray"/>
                <TextBlock x:Name="ColdStartPassRateText" Text="—" FontSize="20" Margin="0,0,0,10"/>

                <TextBlock Text="Warm Start Pass Rate" Foreground="Gray"/>
                <TextBlock x:Name="WarmStartPassRateText" Text="—" FontSize="20" Margin="0,0,0,10"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="5">
                <TextBlock Text="Timing Averages" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>

                <TextBlock Text="Avg Cold Start" Foreground="Gray"/>
                <TextBlock x:Name="AvgColdStartText" Text="—" FontSize="20" Margin="0,0,0,10"/>

                <TextBlock Text="Avg Warm Start" Foreground="Gray"/>
                <TextBlock x:Name="AvgWarmStartText" Text="—" FontSize="20" Margin="0,0,0,10"/>

                <TextBlock Text="P95 Cold Start" Foreground="Gray"/>
                <TextBlock x:Name="P95ColdStartText" Text="—" FontSize="20" Margin="0,0,0,10"/>

                <TextBlock Text="P95 Warm Start" Foreground="Gray"/>
                <TextBlock x:Name="P95WarmStartText" Text="—" FontSize="20" Margin="0,0,0,10"/>
            </StackPanel>
        </Grid>

        <!-- Actions -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="5">
            <Button x:Name="ViewHistoryButton" Content="View History" Click="ViewHistoryButton_Click"
                    Padding="15,10" Margin="0,0,10,0"/>
            <Button x:Name="ExportReportButton" Content="Export Report" Click="ExportReportButton_Click"
                    Padding="15,10"/>
        </StackPanel>
    </Grid>
</Page>
