<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!-- Visual Effects -->
    <DropShadowEffect x:Key="ShadowEffect" BlurRadius="15" ShadowDepth="0" Color="#44000000" Opacity="0.4"/>
    
    <!--
        Acrylic/Glass Effects Theme Extension for Redball v3.0
        Adds Windows 11-style acrylic backgrounds and modern animations
    -->
    
    <!-- Acrylic Background Brush (requires Windows 10 1803+ or Windows 11) -->
    <SolidColorBrush x:Key="AcrylicBackgroundBrush" Color="#F3202020"/>
    <SolidColorBrush x:Key="AcrylicSecondaryBrush" Color="#E62D2D2D"/>
    <SolidColorBrush x:Key="AcrylicTertiaryBrush" Color="#CC3C3C3C"/>
    
    <!-- WCAG AA Compliant Colors - Higher contrast for accessibility -->
    <SolidColorBrush x:Key="AccessibleForeground" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="AccessibleForegroundDim" Color="#B3FFFFFF"/>
    <SolidColorBrush x:Key="AccessibleAccent" Color="#FFFF6B6B"/>
    <SolidColorBrush x:Key="AccessibleSuccess" Color="#FF6BCB77"/>
    <SolidColorBrush x:Key="AccessibleWarning" Color="#FFFFD93D"/>
    <SolidColorBrush x:Key="AccessibleError" Color="#FFFF6B6B"/>
    
    <!-- Micro-interactions: Button Hover Animation -->
    <Style x:Key="AnimatedButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Style.Triggers>
            <EventTrigger RoutedEvent="MouseEnter">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" 
                                         To="1.02" Duration="0:0:0.15">
                            <DoubleAnimation.EasingFunction>
                                <QuadraticEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" 
                                         To="1.02" Duration="0:0:0.15">
                            <DoubleAnimation.EasingFunction>
                                <QuadraticEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
            <EventTrigger RoutedEvent="MouseLeave">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" 
                                         To="1" Duration="0:0:0.2">
                            <DoubleAnimation.EasingFunction>
                                <QuadraticEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" 
                                         To="1" Duration="0:0:0.2">
                            <DoubleAnimation.EasingFunction>
                                <QuadraticEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
            <EventTrigger RoutedEvent="Click">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" 
                                         To="0.98" Duration="0:0:0.05" AutoReverse="True">
                            <DoubleAnimation.EasingFunction>
                                <QuadraticEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" 
                                         To="0.98" Duration="0:0:0.05" AutoReverse="True">
                            <DoubleAnimation.EasingFunction>
                                <QuadraticEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>
    
    <!-- Micro-interactions: Card Hover Lift -->
    <Style x:Key="LiftCardStyle" TargetType="Border" BasedOn="{StaticResource CardStyle}">
        <Setter Property="RenderTransform">
            <Setter.Value>
                <TranslateTransform Y="0"/>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <EventTrigger RoutedEvent="MouseEnter">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.Y" 
                                         To="-3" Duration="0:0:0.2">
                            <DoubleAnimation.EasingFunction>
                                <QuadraticEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                                         To="1" Duration="0:0:0.2"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
            <EventTrigger RoutedEvent="MouseLeave">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.Y" 
                                         To="0" Duration="0:0:0.2">
                            <DoubleAnimation.EasingFunction>
                                <QuadraticEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>
    
    <!-- Enhanced Tooltip Style with Fade -->
    <Style x:Key="EnhancedToolTip" TargetType="ToolTip">
        <Setter Property="Background" Value="{DynamicResource AcrylicSecondaryBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource AccessibleForeground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Opacity" Value="0"/>
        <Style.Triggers>
            <Trigger Property="IsOpen" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                                             To="1" Duration="0:0:0.2"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                                             To="0" Duration="0:0:0.15"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
        </Style.Triggers>
    </Style>
    
    <!-- WCAG AA Compliant Focus Visual -->
    <Style x:Key="AccessibleFocusVisual">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Rectangle Stroke="{DynamicResource AccessibleAccent}" 
                               StrokeThickness="2" 
                               StrokeDashArray="2 2"
                               RadiusX="4" 
                               RadiusY="4"
                               Margin="-2"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Accessible Button Style -->
    <Style x:Key="AccessibleButton" TargetType="Button" BasedOn="{StaticResource AnimatedButton}">
        <Setter Property="FocusVisualStyle" Value="{StaticResource AccessibleFocusVisual}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="MinHeight" Value="36"/>
        <Setter Property="MinWidth" Value="80"/>
        <Setter Property="Foreground" Value="{DynamicResource AccessibleForeground}"/>
    </Style>
    
    <!-- Accessible TextBlock for better readability -->
    <Style x:Key="AccessibleText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource AccessibleForeground}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="LineHeight" Value="20"/>
    </Style>
    
    <!-- Loading/Progress Animation -->
    <Storyboard x:Key="LoadingAnimation" RepeatBehavior="Forever">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" 
                         From="0" To="360" Duration="0:0:1"/>
    </Storyboard>
    
    <!-- Success Checkmark Animation -->
    <Storyboard x:Key="SuccessAnimation">
        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3"/>
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" 
                         From="0" To="1.2" Duration="0:0:0.3">
            <DoubleAnimation.EasingFunction>
                <ElasticEase Oscillations="1" Springiness="5"/>
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" 
                         From="0" To="1.2" Duration="0:0:0.3">
            <DoubleAnimation.EasingFunction>
                <ElasticEase Oscillations="1" Springiness="5"/>
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" 
                         From="1.2" To="1" Duration="0:0:0.15" BeginTime="0:0:0.3"/>
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" 
                         From="1.2" To="1" Duration="0:0:0.15" BeginTime="0:0:0.3"/>
    </Storyboard>

</ResourceDictionary>
